/**
 * @desc the initial state on the application
*/
const initialState = {
  auth: {
    signup: {
      isAuth: false,
      registered: false,
      progress: false,
    },
    user: {

    },
    isAuth: false,
  },
};

export default initialState;
